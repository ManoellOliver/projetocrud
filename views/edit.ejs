<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Meu editar</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            padding: 20px;
        }

        h2 {
            color: #333;
            text-align: center;
        }

        form {
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 5px;
            padding: 20px;
            margin-top: 20px;
        }

        input[type="text"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
        }
    </style>
</head>

<body>
    <!-- <h2>Editar Registro</h2>
    <% crud.forEach(function(details) { %>
    <form action="/edit/<%= details._id %>" method="POST">
        <input type="text" value="<%= details.name %>" name="name">
        <input type="text" value="<%= details.surname %>" name="surname">
        <br/>
        <button><a href="/show">Todos</a></button>
        <button type="submit">Salvar</button>
    </form>
    <% }) %> -->

    <h2>Editar Registro</h2>
<% crud.forEach(function(details) { %>
    <form action="/edit/<%= details._id %>" method="POST">
        <label for="name">Nome completo:</label>
        <input type="text" value="<%= details.name %>" name="name" required>

        <label for="email">E-mail:</label>
        <input type="email" value="<%= details.email %>" name="email" required>

        <label for="dataNascimento">Data de Nascimento:</label>
        <input type="date" value="<%= details.dataNascimento %>" name="dataNascimento" required>
        <br/>

        <!-- Restante do formulário de cadastro... -->

        <label for="password">Senha:</label>
        <input type="password" id="password" name="password" required>
        <span id="password-error" class="error-message"></span>

        <label for="confirm-password">Confirmar Senha:</label>
        <input type="password" id="confirm-password" name="confirm-password" required>
        <span id="confirm-password-error" class="error-message"></span>

        <!-- ... outros campos ... -->

        <button><a href="/show">Todos</a></button>
        <button type="submit">Salvar</button>
    </form>
<% }) %>
</body>

</html>
